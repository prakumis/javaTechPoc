<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:p="http://www.springframework.org/schema/p" 
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="
        http://www.springframework.org/schema/beans 
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context 
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc 
        http://www.springframework.org/schema/mvc/spring-mvc.xsd">


    <!-- <bean id="handlerMapping"
        class="org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping">
        <property name="interceptors">
            <list>
                <ref bean="customRequestHandler" />
            </list>
        </property>
    </bean> -->

    <!-- Configures the annotation-driven Spring MVC Controller -->
	<mvc:annotation-driven />

	<!-- Scan for Controllers -->
	<context:component-scan base-package="com.nyp.shopping.web" />
	<mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/**" />
            <bean class="com.nyp.shopping.web.interceptor.CustomRequestHandler" />
        </mvc:interceptor>
        <mvc:interceptor>
            <mvc:mapping path="/l10n/switchLocale" />
            <bean id="localeChangeInterceptor" class="org.springframework.web.servlet.i18n.LocaleChangeInterceptor" >
                <property name="paramName" value="language" />
            </bean>
        </mvc:interceptor>
	</mvc:interceptors>

    <!-- Start:: Support for i18n 
        More Read: 
            http://www.mkyong.com/spring-mvc/spring-mvc-internationalization-example/
            https://www.mkyong.com/spring-mvc/cannot-change-http-accept-header-use-a-different-locale-resolution-strategy/
    -->
    <bean id="localeResolver"
        class="org.springframework.web.servlet.i18n.SessionLocaleResolver">
        <property name="defaultLocale" value="en" />
    </bean>
    <!-- End:: Support for i18n -->

	<!-- Error View Page is not required as RestResponseEntityExceptionHandler 
		is doing the error handling job. Again this is REST service so there is no 
		view, only data is transferred. -->
	<!-- <bean
		class="org.springframework.web.servlet.handler.SimpleMappingExceptionResolver">
		<property name="defaultErrorView" value="error" />
	</bean> -->

	<!-- Handles GET requests for /resources/** by efficiently serving static 
		content in the ${webappRoot}/resources dir -->
	<mvc:resources mapping="/resources/**" location="/resources/" />
	<mvc:resources mapping="/favicon.ico" location="/favicon.ico" />

	<!-- Added for json support in spring 4.1.4 
	<bean name="jsonTemplate" class="org.springframework.web.servlet.view.json.MappingJackson2JsonView"/>-->


</beans>